{
  "$comment": [
    "Which rules from .agents/cg/principles/ bind which folder.",
    "AUTHORED BY HAND. No check proves a scope is correct — the verifier only proves a rule ID exists.",
    "A wrongly narrow scope means a folder silently stops being told about a rule it must obey.",
    "When unsure, include the rule.",
    "",
    "Only AP-* and PP-* are ever inherited. The fork-loaded families — DP-*, OP-*, UP-*, SP-* —",
    "are topic-scoped and loaded at a fork, never stamped into a contract, and `cg verify`",
    "rejects an entry naming one: an unavoidable guide is just a rule.",
    "",
    "Key   — folder path relative to the repository root.",
    "depth — number of path segments in the key. Verified against the key.",
    "kind  — 'module' (a workspace root: needs CLAUDE.md and AGENTS.md pointers) or 'folder'.",
    "contract — path to the contract.md that receives the generated inherited-rules block.",
    "rules — the rule IDs that bind this folder.",
    "",
    "Run `cg sync` after editing, then `cg verify`.",
    "",
    "'folders' is empty in a repository that already has code — run the `cg-warmup` skill once",
    "and it fills this map from the modules it finds. A greenfield scaffold ships one worked",
    "example entry instead; replace it with your own."
  ],
  "folders": {
    "src": {
      "depth": 1,
      "kind": "module",
      "contract": "src/.agents/cg/contract.md",
      "rules": [
        "AP-01-01",
        "AP-01-02",
        "AP-02-01",
        "AP-02-02"
      ]
    }
  }
}
