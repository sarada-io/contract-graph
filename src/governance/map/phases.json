{
  "_comment": "Which rule families each lifecycle phase loads. `always` families are inherited into every contract and are read on every task. `conditional` families are loaded only when the work touches that domain — an unavoidable guide is just a rule, which is why they are never inherited. Every family that ships must be named by at least one phase; `cg verify` fails otherwise.",
  "phases": {
    "plan": {
      "always": ["AP", "PP"],
      "conditional": ["DP", "UP", "SP"]
    },
    "prepare": {
      "always": ["AP", "PP"],
      "conditional": ["DP", "UP", "OP", "SP"]
    },
    "produce": {
      "always": ["AP", "PP"],
      "conditional": ["OP", "UP", "SP"]
    },
    "sign-off": {
      "always": ["AP", "PP"],
      "conditional": ["OP", "SP"]
    },
    "unblock": {
      "always": ["AP", "PP"],
      "conditional": ["DP", "UP", "OP", "SP"]
    }
  }
}
