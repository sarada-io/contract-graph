interface:
  display_name: "CG Unblock"
  short_description: "Log blockers and keep ready Steps moving"
  default_prompt: "Use $cg-unblock to classify this fork against D-1, record assumptions or log protected decisions in the profile docs decision log, keep dependency-safe Ready work moving, and name the user's exact next action and skill without invoking it."
