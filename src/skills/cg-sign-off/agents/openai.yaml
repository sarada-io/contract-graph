interface:
  display_name: "CG Sign Off"
  short_description: "Finish a prototype or close a delivery phase"
  default_prompt: "Use $cg-sign-off to resolve the intended programme and sign-off type, then load its procedure to finish the selected prototype or close the selected delivery phase; ask for selection if the target is ambiguous."
