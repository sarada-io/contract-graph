# src — agent entry point

This folder is openable as a workspace root on its own.

**Start here: `.agents/cg/contract.md`** — how this module is used by the project, its boundary,
entry points, and the child contracts that locate narrower responsibilities. Traverse those
contracts before reading implementation. The contract repeats the repository rules that bind this
folder, so you do not need the repository root to work here correctly.

For the full text of any inherited rule, read `../.agents/cg/principles/`.
For the lifecycle workflow, read `../.agents/cg/workflow.md`.

Do not put instructions in this file.
