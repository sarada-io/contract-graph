# src — agent entry point

This folder is openable as a workspace root on its own.

**Start here: `.agents/cg/contract.md`** — this module's boundary, invariants, and entry points.
It repeats the repository rules that bind this folder, so you do not need the repository root to
work here correctly.

For the full text of any inherited rule, read `../.agents/cg/principles/architecture.md`.
For the lifecycle workflow, read `../.agents/cg/workflow.md`.

Do not put instructions in this file.
