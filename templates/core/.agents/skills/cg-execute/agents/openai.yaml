interface:
  display_name: "Contract Graph Execute"
  short_description: "Run ready Steps serially until blocked or done"
  default_prompt: "Use $cg-execute to run the earliest Ready Step with implementation, tests, contracts, detectors, and gates, recalculate the queue after each verified handoff, and continue serially until all Steps complete or no Ready work remains; then name the user's exact next action and skill."
