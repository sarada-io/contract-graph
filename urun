#!/bin/sh
# Repository-local menu. All behaviour lives in scripts/urun.mjs.
root="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)" || exit 1
if ! command -v node >/dev/null 2>&1; then
  echo 'urun needs Node.js 18.17 or newer.' >&2
  exit 1
fi
exec node "$root/scripts/urun.mjs" "$@"
