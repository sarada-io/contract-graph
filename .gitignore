node_modules/
npm-debug.log*
*.tgz
.DS_Store
.claude/settings.local.json

# Generated working trees — `npm run clean` deletes them
plan/
# Maintainer planning and review scratch are local, not product documentation.
/docs/plan/
# Test scratch runs live under tmp/tests/ and are removed by the test runner.
tmp/
# Legacy generated targets
build/
compiled/
# Assembled package in dist/build/ and release archives in dist/tar/
dist/

# Test output when a host sets its temporary directory to the checkout.
# mkdtemp appends six random characters; do not ignore all cg-* paths.
/cg-*-[A-Za-z0-9][A-Za-z0-9][A-Za-z0-9][A-Za-z0-9][A-Za-z0-9][A-Za-z0-9]/
/cg-gate-*.json
/cg-npm-cache/
/node-compile-cache/
